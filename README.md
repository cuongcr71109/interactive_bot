# Interact Tool for Telegram



Using CMD. If you are using PowerShell, type 'cmd' to switch to CMD
1. Create new virtual environment
` py -m venv venv`
2. Activate new environment
` venv\Scripts\activate `
3. Install required modules
`requirements`
4. Run project
`run`
